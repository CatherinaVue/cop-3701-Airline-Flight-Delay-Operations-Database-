# Final Revised ER Diagram (BCNF)

![Final ER Diagram](partC.png)

# Final Normalized Relational Schema

AIRLINE(AIR_ID, AIR_NAME, AIR_CODE)

AIRPORT(AIRP_ID, AIRP_NAME, AIRP_CODE, STATE_CODE)

FLIGHTS(FLIGHT_ID, AIR_ID, DEPART_AIRP_ID, ARRIVAL_AIRP_ID,
        FLIGHT_DATE,
        FLIGHT_SCH_DEPART,
        FLIGHT_ACT_DEPART,
        FLIGHT_SCH_ARRIVAL,
        FLIGHT_ACT_ARRIVAL)

DELAYS(DELAY_ID, FLIGHT_ID, DELAY_REASON, DELAY_MINUTES)

AIRLINE_AIRPORT(AIR_ID, AIRP_ID)
